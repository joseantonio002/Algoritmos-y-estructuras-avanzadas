#include <iostream>
#include <vector>
using namespace std;

int main() {
  vector<int> sec = {5,6,3,2,34,65,32,67,4,32,57, -1};
  int size = sec.size();
  int j, x, med, ini, fin;
for (int i = 1; i < size; i++ ) {
   j = i ;
   x = sec[i] ;
   ini = 0 ; fin = i-1; //comprueba del inicio a ese -1
   while ( ini <= fin ) {
      med = (ini+fin)/2; 
      if (sec[med] < x)
         ini = med+1;
      else
         fin = med-1;
   } 
   for (int j = i-1; j >= ini; j--)
      sec[j+1] = sec[j] ;
   sec[ini] = x ;
} 
  for(int i = 0; i < size; i++) {
    cout << sec[i] << ' ';
  }
  return 0;
}
